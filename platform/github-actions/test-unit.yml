# Shared GitHub Actions — Unit Test Workflow
name: Unit Tests

on:
  workflow_call:
    inputs:
      project:
        description: 'Project to test (veyor or biomedical)'
        required: true
        type: string

jobs:
  test-typescript:
    name: Test TypeScript
    runs-on: ubuntu-latest
    if: inputs.project == 'veyor'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: projects/veyor-marketplace/frontend/package-lock.json
      - run: cd projects/veyor-marketplace/frontend && npm ci
      - name: Run tests with 80% coverage threshold
        run: cd projects/veyor-marketplace/frontend && npm test -- --coverage --watchAll=false --coverageThreshold='{"global":{"lines":80}}'
      - uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: projects/veyor-marketplace/frontend/coverage/

  test-java:
    name: Test Java
    runs-on: ubuntu-latest
    if: inputs.project == 'veyor'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'
      - run: cd projects/veyor-marketplace/backend && ./gradlew test
      - uses: actions/upload-artifact@v4
        with:
          name: backend-test-results
          path: projects/veyor-marketplace/backend/build/reports/tests/

  test-python:
    name: Test Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Test Veyor Agents
        if: inputs.project == 'veyor'
        run: |
          cd projects/veyor-marketplace/agents
          pip install -r requirements.txt
          pip install pytest pytest-cov
          python -m pytest tests/ -v --cov=. --cov-report=xml --cov-fail-under=80
      - name: Test BioMedical
        if: inputs.project == 'biomedical'
        run: |
          cd projects/biomedical-ai
          pip install -e ".[test]"
          python -m pytest tests/unit/ -v --cov=src --cov-report=xml --cov-fail-under=80

  test-go:
    name: Test Go
    runs-on: ubuntu-latest
    if: inputs.project == 'veyor'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Test Quoting Service
        run: |
          cd projects/veyor-marketplace/quoting-service
          go test ./... -v -race -coverprofile=coverage.out
          coverage=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | tr -d '%')
          echo "Quoting Service Coverage: $coverage%"
          if [ ! -z "$coverage" ]; then
            if awk "BEGIN {exit !($coverage < 80.0)}"; then
              echo "❌ Code coverage is $coverage%, which is below the 80% threshold!"
              exit 1
            fi
          fi
      - name: Test Carrier Simulator
        run: |
          cd projects/veyor-marketplace/carrier-simulator
          go test ./... -v -race -coverprofile=coverage.out
          coverage=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | tr -d '%')
          echo "Carrier Simulator Coverage: $coverage%"
          if [ ! -z "$coverage" ]; then
            if awk "BEGIN {exit !($coverage < 80.0)}"; then
              echo "❌ Code coverage is $coverage%, which is below the 80% threshold!"
              exit 1
            fi
          fi

