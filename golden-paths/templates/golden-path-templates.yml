# ==============================================================================
# Backstage — Golden Path Template: New Microservice
# ==============================================================================
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: new-microservice
  title: 🚀 New Microservice
  description: Create a new microservice with Dockerfile, Helm chart, CI/CD, tests, and observability — following platform golden paths
  tags:
    - golden-path
    - microservice
    - recommended
spec:
  owner: polfenollar
  type: service

  parameters:
    - title: Service Configuration
      required:
        - name
        - language
        - project
      properties:
        name:
          title: Service Name
          type: string
          description: Name of the new microservice (kebab-case)
          pattern: '^[a-z][a-z0-9-]*$'
          ui:autofocus: true
        description:
          title: Description
          type: string
          description: Brief description of what this service does
        language:
          title: Language
          type: string
          description: Primary programming language
          enum:
            - python
            - go
            - java
            - typescript
          enumNames:
            - Python (FastAPI)
            - Go (gRPC/REST)
            - Java (Spring Boot)
            - TypeScript (NestJS)
        project:
          title: Project
          type: string
          description: Which project does this service belong to?
          enum:
            - veyor
            - biomedical
          enumNames:
            - Veyor Marketplace
            - BioMedical AI

    - title: Infrastructure
      properties:
        needsDatabase:
          title: Needs PostgreSQL Database
          type: boolean
          default: false
        needsCache:
          title: Needs Redis Cache
          type: boolean
          default: false
        needsMessageQueue:
          title: Needs Kafka / Message Queue
          type: boolean
          default: false
        port:
          title: Service Port
          type: integer
          default: 8080

  steps:
    - id: fetch-base
      name: Fetch Base Skeleton
      action: fetch:template
      input:
        url: ./new-microservice/skeleton-base
        values:
          name: ${{ parameters.name }}
          description: ${{ parameters.description }}
          project: ${{ parameters.project }}
          port: ${{ parameters.port }}
          needsDatabase: ${{ parameters.needsDatabase }}
          needsCache: ${{ parameters.needsCache }}
          needsMessageQueue: ${{ parameters.needsMessageQueue }}

    - id: fetch-python
      name: Fetch Python Skeleton
      action: fetch:template
      if: ${{ parameters.language == 'python' }}
      input:
        url: ./new-microservice/skeleton-python
        values:
          name: ${{ parameters.name }}
          needsDatabase: ${{ parameters.needsDatabase }}
          needsCache: ${{ parameters.needsCache }}
          needsMessageQueue: ${{ parameters.needsMessageQueue }}

    - id: fetch-go
      name: Fetch Go Skeleton
      action: fetch:template
      if: ${{ parameters.language == 'go' }}
      input:
        url: ./new-microservice/skeleton-go
        values:
          name: ${{ parameters.name }}

    - id: fetch-java
      name: Fetch Java Skeleton
      action: fetch:template
      if: ${{ parameters.language == 'java' }}
      input:
        url: ./new-microservice/skeleton-java
        values:
          name: ${{ parameters.name }}

    - id: fetch-typescript
      name: Fetch TypeScript Skeleton
      action: fetch:template
      if: ${{ parameters.language == 'typescript' }}
      input:
        url: ./new-microservice/skeleton-typescript
        values:
          name: ${{ parameters.name }}

    - id: publish-github
      name: Publish to GitHub
      action: publish:github
      input:
        allowedHosts: ['github.com']
        repoUrl: github.com?owner=polfenollar&repo=${{ parameters.name }}
        description: ${{ parameters.description }}
        defaultBranch: main
        protectDefaultBranch: true
        requireCodeOwnerReviews: true

    - id: register-catalog
      name: Register in Backstage
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps['publish-github'].output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: ${{ steps['publish-github'].output.remoteUrl }}
      - title: Open in Backstage
        icon: catalog
        entityRef: ${{ steps['register-catalog'].output.entityRef }}
---
# ==============================================================================
# Backstage — Golden Path Template: New AI Agent
# ==============================================================================
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: new-ai-agent
  title: 🤖 New AI Agent
  description: Create a new LangChain/LangGraph AI agent with Langsmith tracing, Temporal workflow, and observability
  tags:
    - golden-path
    - ai-agent
    - langchain
spec:
  owner: polfenollar
  type: service

  parameters:
    - title: Agent Configuration
      required:
        - name
        - agentFramework
        - project
      properties:
        name:
          title: Agent Name
          type: string
          pattern: '^[a-z][a-z0-9-]*$'
        description:
          title: Description
          type: string
        agentFramework:
          title: Agent Framework
          type: string
          enum:
            - langchain-react
            - langgraph
            - crewai
          enumNames:
            - LangChain ReAct Agent
            - LangGraph State Machine
            - CrewAI Multi-Agent
        project:
          title: Project
          type: string
          enum:
            - veyor
            - biomedical

    - title: Agent Capabilities
      properties:
        enableLangsmith:
          title: Enable Langsmith Tracing
          type: boolean
          default: true
        enableTemporal:
          title: Enable Temporal Workflow
          type: boolean
          default: false
        tools:
          title: Agent Tools
          type: array
          items:
            type: string
            enum:
              - database-query
              - api-call
              - vector-search
              - web-search
              - file-reader

  steps:
    - id: fetch-base
      name: Fetch Agent Base Skeleton
      action: fetch:template
      input:
        url: ./new-ai-agent/skeleton-base
        values:
          name: ${{ parameters.name }}
          description: ${{ parameters.description }}
          project: ${{ parameters.project }}

    - id: fetch-langchain-react
      name: Fetch LangChain ReAct Skeleton
      action: fetch:template
      if: ${{ parameters.agentFramework == 'langchain-react' }}
      input:
        url: ./new-ai-agent/skeleton-langchain-react
        values:
          name: ${{ parameters.name }}
          project: ${{ parameters.project }}
          enableLangsmith: ${{ parameters.enableLangsmith }}

    - id: fetch-langgraph
      name: Fetch LangGraph Skeleton
      action: fetch:template
      if: ${{ parameters.agentFramework == 'langgraph' }}
      input:
        url: ./new-ai-agent/skeleton-langgraph
        values:
          name: ${{ parameters.name }}
          project: ${{ parameters.project }}
          enableLangsmith: ${{ parameters.enableLangsmith }}

    - id: fetch-crewai
      name: Fetch CrewAI Skeleton
      action: fetch:template
      if: ${{ parameters.agentFramework == 'crewai' }}
      input:
        url: ./new-ai-agent/skeleton-crewai
        values:
          name: ${{ parameters.name }}
          project: ${{ parameters.project }}
          enableLangsmith: ${{ parameters.enableLangsmith }}

    - id: fetch-temporal
      name: Fetch Temporal Workflow Addon
      action: fetch:template
      if: ${{ parameters.enableTemporal }}
      input:
        url: ./new-ai-agent/skeleton-temporal
        values:
          name: ${{ parameters.name }}

    - id: publish-github
      name: Publish to GitHub
      action: publish:github
      input:
        allowedHosts: ['github.com']
        repoUrl: github.com?owner=polfenollar&repo=${{ parameters.name }}
        description: ${{ parameters.description }}
        defaultBranch: main

    - id: register-catalog
      name: Register in Backstage
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps['publish-github'].output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: ${{ steps['publish-github'].output.remoteUrl }}
      - title: Open in Backstage
        icon: catalog
        entityRef: ${{ steps['register-catalog'].output.entityRef }}
---
# ==============================================================================
# Backstage — Golden Path Template: New Frontend
# ==============================================================================
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: new-frontend
  title: 🎨 New Frontend App
  description: Create a new Next.js frontend with ESLint, Playwright E2E tests, and deployment config
  tags:
    - golden-path
    - frontend
    - nextjs
spec:
  owner: polfenollar
  type: website

  parameters:
    - title: Frontend Configuration
      required:
        - name
      properties:
        name:
          title: Application Name
          type: string
          pattern: '^[a-z][a-z0-9-]*$'
        description:
          title: Description
          type: string
        styling:
          title: Styling Framework
          type: string
          enum:
            - css-modules
            - tailwind
            - styled-components
          default: css-modules

  steps:
    - id: fetch-base
      name: Fetch Frontend Base Skeleton
      action: fetch:template
      input:
        url: ./new-frontend/skeleton-base
        values:
          name: ${{ parameters.name }}
          description: ${{ parameters.description }}
          styling: ${{ parameters.styling }}

    - id: publish-github
      name: Publish to GitHub
      action: publish:github
      input:
        allowedHosts: ['github.com']
        repoUrl: github.com?owner=polfenollar&repo=${{ parameters.name }}
        description: ${{ parameters.description }}

    - id: register-catalog
      name: Register in Backstage
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps['publish-github'].output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: ${{ steps['publish-github'].output.remoteUrl }}
---
# ==============================================================================
# Backstage — Golden Path Template: New Data Pipeline
# ==============================================================================
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: new-data-pipeline
  title: 📊 New Data Pipeline
  description: Create a data ingestion pipeline with Iceberg, MinIO, quality checks, and Temporal orchestration
  tags:
    - golden-path
    - data-pipeline
    - lakehouse
spec:
  owner: polfenollar
  type: service

  parameters:
    - title: Pipeline Configuration
      required:
        - name
        - dataSource
      properties:
        name:
          title: Pipeline Name
          type: string
          pattern: '^[a-z][a-z0-9-]*$'
        description:
          title: Description
          type: string
        dataSource:
          title: Data Source
          type: string
          enum:
            - pubmed
            - clinicaltrials
            - custom-api
            - file-upload
          enumNames:
            - PubMed Open Access
            - ClinicalTrials.gov
            - Custom REST API
            - File Upload (CSV/JSON)
        qualityChecks:
          title: Enable Data Quality Checks
          type: boolean
          default: true

  steps:
    - id: fetch-base
      name: Fetch Pipeline Base Skeleton
      action: fetch:template
      input:
        url: ./new-data-pipeline/skeleton-base
        values:
          name: ${{ parameters.name }}
          description: ${{ parameters.description }}
          dataSource: ${{ parameters.dataSource }}
          qualityChecks: ${{ parameters.qualityChecks }}

    - id: publish-github
      name: Publish to GitHub
      action: publish:github
      input:
        allowedHosts: ['github.com']
        repoUrl: github.com?owner=polfenollar&repo=${{ parameters.name }}

    - id: register-catalog
      name: Register in Backstage
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps['publish-github'].output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: ${{ steps['publish-github'].output.remoteUrl }}
